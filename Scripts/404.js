var loaded = "Website has been successfully loaded";
var errorMsg = "This page cannot be found.";

// Notice that the page has been loaded
console.log(loaded);

// Notice that the page cannot be found
console.warn(errorMsg);